import { TVocabularies } from '../types/vocabularies.types';
import ru from './ru';
import en from './en';

const vocabularies : TVocabularies = {
  ru,
  en,
};
export const defaultLang = 'ru';
export default vocabularies;
